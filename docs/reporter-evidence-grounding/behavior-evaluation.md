# Prepared behavioral evaluation

This plan requires a separately authorized paid run. Implementation tests use
frozen SQLite fixtures and scripted completions; they establish mechanical
contracts, not season-level editorial improvement or general semantic truth.

Use the season-simulation operator workflow to prepare a new isolated campaign
from the same source-only observations, mappings and empty reporter memory as the
retained baseline. Pin the baseline and candidate code, snapshot derivation
version, prepared reporter definition, model/provider and actual reasoning
parameters. Do not mix code versions midway through a campaign. Keep observation
timestamps honest and simulated editorial time separate. Never reset the user's
application database. Export artifacts before stopping disposable infrastructure.

Run a short revealing prefix before a full sequential season. Include a
continuity request crossing a renamed franchise and these reviewed cases:

| Case | Inspect against executed source evidence |
| --- | --- |
| Trade continuity | Kenneth Walker sent, DJ Moore and a first received; net pick gain is not described as spending flexibility without separate valuation evidence |
| Rename | Current franchise reaches both actual season rosters through supplied lookup arguments; failed recovery is disclosed |
| Records | Correct team owns 10–18 versus 9–19; points display rounds 1754.9999999999998 to 1755.00 |
| Superlative | Asserted min/max and any uniqueness match the complete requested population; distinguish current streak from historical record |
| Preseason | Unplayed/unknown games yield no fabricated results, ties, streaks or ranks; separately retain the focused true-zero fixture |
| Championship | Winners-bracket championship outcome is the support; standings or memory alone are insufficient |
| Before/after | Listed roster membership compares actual cutoff observations, with limits on acquisition timing, method and completeness |
| Revisions | Draft and brief edits invalidate applicable verification receipts; unsupported prose patterns remain advisory |

For each step export article, structured brief, public tool results, full private
tool metadata, definition identity, provider parameters/usage and memory revision.
Record failed drill-downs, source/claim mismatches, unsupported interpretations,
visible caveats, diagnostic repair attempts, completion/loop rate, result size,
tokens and cost. Compare package-level outcomes with the frozen baseline. Review
actual prose, including claims that were never voluntarily entered in the brief.

Do not call a valid source receipt proof that every sentence follows from evidence.
Review whether advisory diagnostics lead to useful corrections without excessive
loops or omission of useful interpretation. Keep memory identity/update/search
limitations in their separate deferred workstream.
